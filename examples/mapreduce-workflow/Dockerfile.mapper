FROM python:3.9-slim

WORKDIR /app

# Install required packages
RUN pip install --no-cache-dir requests redis beautifulsoup4

# Copy the mapper script
COPY mapper.py .

# Set environment for the workflow
ENV PYTHONUNBUFFERED=1

CMD ["python", "mapper.py"]